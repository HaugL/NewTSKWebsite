<% @recent_orders.each do |order| %>
<%= form_for order do |f| %>
	
	Product: <%= Product.find(order.product_id).name %>, Quantity: <%= order.quantity %>, <%= order.created_at.strftime("Date Placed: %A, %b %d") %>, <%= order.expected_pickup_date.strftime("Expected Pickup Day: %A, %b %d")%>

	<%= f.hidden_field :ready_for_pickup, :value => true %>
	<%= f.submit 'Mark Product As Ready For Pickup' %>
<% end %>
	<br>  
<% end %>
PAST SHTUFF
<br>
<% @past_orders.each do |order| %>
	Product: <%= Product.find(order.product_id).name %>, Quantity: <%= order.quantity %>, <%= order.created_at.strftime("Date Order Placed: %A, %b %d") %>, <%= order.updated_at.strftime("Date Put Out For Pickup: %A, %b %d") %>
	<br>  
<% end %>