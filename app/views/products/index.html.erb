<%= render "cart/cart" %>

<div class = 'index-container'>
	<% @products.each do |product| %>
		<div class = 'product-container'>
			<div class ='product-title'><%= link_to product.name, product_path(product) %></div>
			<%= image_tag product.image.url(:thumb)%>
		</div>

	<% end %>
</div>

